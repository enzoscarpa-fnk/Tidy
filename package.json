{
  "name": "tidy",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev:backend": "pnpm --filter @tidy/backend dev",
    "build:backend": "pnpm --filter @tidy/backend build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@fastify/multipart": "^9.4.0"
  }
}
